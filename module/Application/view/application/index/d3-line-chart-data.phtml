<?php
//    echo implode(',', $this->header) . "\n";
//    $data = $this->data;
//    while (count($data) > 0) {
//        $row = array_shift($data);
//        $csv = implode(',', $row);
//        $line = count($data) > 0 ? $csv . "\n" : $csv;
//        echo $line;
//    }
echo json_encode($this->data);